import React from "react";
import CartComponent from "@/components/pages/rescue-points/cart/cart.component";

export const metadata = {
  title: "Sacola de Resgate",
};

export default function CartPage() {
  return <CartComponent />;
}
